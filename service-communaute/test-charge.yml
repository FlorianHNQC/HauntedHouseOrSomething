config:
  target: "http://localhost:3000"
  phases:
    - duration: 30
      arrivalRate: 20
      rampTo: 100
      name: "Afflux massif de passionnés d'horreur"

scenarios:
  - name: "Création d'un article communautaire (Simulé)"
    flow:
      - get:
          url: "/api/echanges/articles"